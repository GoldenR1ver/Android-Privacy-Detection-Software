# Stdlib-only for run_prepare.py (Ollama via urllib).
#
# plot_experiment.py
matplotlib>=3.8.0

# Optional: 讲义中的句子嵌入与聚类拓展（见仓库 WEEK_4/requirements-optional.txt）
# pip install -r ../requirements-optional.txt
